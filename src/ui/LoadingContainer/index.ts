import { memo } from '@app/tools';
import { LoadingContainer as Component } from './LoadingContainer';

const LoadingContainer = memo(Component);

export * from './LoadingContainer.types';
export { LoadingContainer };
